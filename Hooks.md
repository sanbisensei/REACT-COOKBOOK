# Hooks

- React hooks are simple functions that we can use to separate out the reusable part from a functional components.
- It can be stateful and can manage side effects.
- Hooks are special React functions that some certain react feathers than the plain utility functions.
